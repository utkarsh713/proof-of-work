# Drop your media here

No media files were attached to the build request, so the site ships with
elegant dark placeholder panels (grain + diagonal hatch) everywhere a video
or image belongs. Nothing is broken — just drop your real files into this
folder using the **exact filenames** below and reload. No code changes needed.

## Videos (mp4, muted/autoplay-safe, ideally < 8MB each, 16:9 or wider)
- `hero.mp4` — full-screen hero background
- `road.mp4` — road infrastructure panel
- `metro.mp4` — urban mobility panel
- `water.mp4` — water infrastructure panel
- `hero-poster.jpg` — optional poster frame shown before hero.mp4 loads

## Images
- `beforeRoad.jpg` — before shot for the drag comparison
- `afterRoad.jpg` — after shot for the drag comparison
- `citizen.jpg` — gallery: citizen evidence
- `infra.jpg` — gallery: infrastructure monitoring
- `pipeline.jpg` — gallery: utility development
- `project-site.jpg` — gallery: site verification (large tile)

Tip: keep videos short and compressed (H.264, ~5–10 seconds looped) so the
autoplay background stays smooth on lower-end laptops during your demo.
